DATABASE_URL=postgres://unggul:unggul_dev_secret@localhost:5432/unggul_axiom
REDIS_URL=redis://localhost:6379
HOST=0.0.0.0
PORT=8080
JWT_SECRET=change_me_in_production_use_min_32_random_chars

# Admin panel credentials (used at login)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_me_to_a_strong_password

# Maximum upload file size in bytes (default 100 MB)
MAX_UPLOAD_SIZE_BYTES=104857600

# AES-256-GCM encryption key for at-rest file encryption
# Hex-encoded 32 bytes (64 hex chars). Generate with: openssl rand -hex 32
# FILE_ENCRYPTION_KEY=
